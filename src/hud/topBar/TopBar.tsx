import { SCENES } from "../../core/constants/scenes";
import { game } from "../../core/Game";

function TopBar() {
  // game.scene.getScene(SCENES.GAME_SCENE).

  return (
    <div id="topBar" className="top_bar-container">
      TopBar
    </div>
  );
}

export default TopBar;
